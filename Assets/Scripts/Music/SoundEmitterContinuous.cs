using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundEmitterContinuous : SoundEmitter
{
    // Start is called before the first frame update
    void Start()
    {
        audiosou
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
